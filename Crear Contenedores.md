# <ins> **Crear un contenedor por comando**</ins>
* _docker run nombrecontenedordedockerhub:version -dp --name_ .
   - -d mantiene el contenedor en segundo plano
   - -p conecta los puertos del contenedor con el sistema nativo
